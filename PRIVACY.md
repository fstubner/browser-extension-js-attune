# Privacy (Attune)

Attune processes audio locally in your browser using the Web Audio API.

- No audio is recorded, stored, or transmitted by Attune.
- No browsing history is collected.
- Settings (like enabled sites) are stored using Chrome Sync via `chrome.storage.sync`.